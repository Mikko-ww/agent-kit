# 示例

## 开始任务

1. 创建 `task_plan.md`、`findings.md` 和 `progress.md`。
2. 填写目标与阶段。
3. 将阶段 1 标记为 `in_progress`。

## 工作过程中

- 有研究发现：更新 `findings.md`。
- 阶段完成：更新 `task_plan.md` 和 `progress.md`。
- 发生错误：同时记录到 `task_plan.md` 和 `progress.md`。

